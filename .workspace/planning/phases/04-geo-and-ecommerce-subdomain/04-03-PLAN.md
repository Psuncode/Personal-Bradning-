---
phase: 04-geo-and-ecommerce-subdomain
plan: "03"
type: execute
wave: 2
depends_on: []
files_modified:
  - src/app/(ecommerce)/ecommerce/page.tsx
  - src/app/(ecommerce)/layout.tsx
autonomous: true
requirements:
  - SUB-03
must_haves:
  truths:
    - "Visitor on ecommerce.philipsun.com sees a full B2B product showcase page — not 'Coming soon'"
    - "Page has a working 'Contact to Inquire' CTA that opens a mailto link to siteConfig.email"
    - "Page shows both product lines: Puno Filter (commercial water filtration) and Smart Sync (smart home / designer spec)"
    - "Page conveys a professional supplier/partner feel — no pricing shown, no shopping cart language"
    - "Page is fully responsive: readable on mobile, tablet, and desktop"
  artifacts:
    - path: "src/app/(ecommerce)/ecommerce/page.tsx"
      provides: "Full ecommerce landing page with all 8 sections per UI-SPEC"
      contains: "Puno Filter"
      min_lines: 150
    - path: "src/app/(ecommerce)/layout.tsx"
      provides: "Updated metadata: title and description reflect B2B product showcase"
      contains: "B2B"
  key_links:
    - from: "src/app/(ecommerce)/ecommerce/page.tsx"
      to: "src/data/site-config.ts"
      via: "siteConfig.email used for mailto CTA href"
      pattern: "siteConfig\\.email"
    - from: "src/app/(ecommerce)/layout.tsx"
      to: "src/app/(ecommerce)/ecommerce/page.tsx"
      via: "layout wraps page in html/body shell with font variables"
      pattern: "Inter.*variable"
---

<objective>
Replace the "Coming soon" placeholder at `src/app/(ecommerce)/ecommerce/page.tsx` with a complete, production-quality B2B ecommerce landing page per the UI-SPEC (D-16 through D-22).

The page showcases two product lines — Puno Filter (commercial water filtration) and Smart Sync (smart home / designer spec) — to B2B buyers, architects, designers, and distributors. No pricing, no cart, no retail language. Professional supplier feel.

Purpose: Fulfills SUB-03 and gives ecommerce.philipsun.com a real presence that surfaces Philip's entrepreneurial work.

Output: Fully implemented single-file RSC page (no "use client") plus updated layout.tsx metadata. No new dependencies — Lucide icons are already installed, BYU design tokens are already in globals.css.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-geo-and-ecommerce-subdomain/04-CONTEXT.md
@.planning/phases/04-geo-and-ecommerce-subdomain/UI-SPEC.md

<!-- Current placeholder files to replace/update -->
@src/app/(ecommerce)/ecommerce/page.tsx
@src/app/(ecommerce)/layout.tsx

<!-- Data source for email CTA -->
@src/data/site-config.ts

<!-- Reference for BYU design tokens and font variables -->
@src/app/globals.css

<interfaces>
<!-- From src/data/site-config.ts: -->
```typescript
export const siteConfig = {
  name: "Philip Sun",
  email: "ps324@byu.edu",
  links: {
    github: "https://github.com/Psuncode",
    linkedin: "https://www.linkedin.com/in/-philipsun/",
    email: "mailto:ps324@byu.edu",
  },
  // ...
};
```

<!-- From src/app/(ecommerce)/layout.tsx (current — update metadata): -->
```typescript
export const metadata: Metadata = {
  title: "Philip Sun — Ecommerce",
  description: "Ecommerce ventures by Philip Sun.",
};
// Body uses: className="min-h-screen flex flex-col font-sans antialiased"
// layout.tsx does NOT currently import Inter/fonts — add it
```

<!-- Available Lucide icons (already installed): Globe, Factory, Handshake, Ruler, Package, Building2, ArrowRight -->

<!-- BYU design tokens available in Tailwind (defined in globals.css @theme inline): -->
<!-- bg-byu-navy, text-byu-navy = #002E5D -->
<!-- bg-byu-blue, text-byu-blue = #0057B8 -->
<!-- bg-byu-light-blue, text-byu-light-blue = #6B9ED2 -->
<!-- bg-byu-sky, text-byu-sky = #C9E1F4 -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update layout.tsx metadata and add font variables</name>
  <files>src/app/(ecommerce)/layout.tsx</files>
  <action>
Update `src/app/(ecommerce)/layout.tsx` with two changes:

Change 1 — Update metadata (per D-17, D-19, D-22):
```typescript
export const metadata: Metadata = {
  title: "Philip Sun — Global Trading | B2B Product Sourcing",
  description:
    "Philip Sun sources and distributes commercial water filtration (Puno Filter) and smart home technology (Smart Sync) to B2B buyers, architects, designers, and distributors worldwide.",
};
```

Change 2 — Add Inter font to the layout body so the ecommerce page uses font-sans correctly. The ecommerce layout is a separate root layout (renders its own html/body). Add Inter font variable matching the main layout pattern:

```typescript
import { Inter } from "next/font/google";

const inter = Inter({
  variable: "--font-inter",
  subsets: ["latin"],
});
```

Add the font variable to the body className:
```typescript
<body className={`${inter.variable} min-h-screen flex flex-col font-sans antialiased bg-[#F8FAFC]`}>
```

Also ensure `../globals.css` is imported — check if it already exists; if not, add it. The path `../globals.css` is correct because layout.tsx is at `src/app/(ecommerce)/layout.tsx` and globals.css is at `src/app/globals.css`.
  </action>
  <verify>
    <automated>npm run build 2>&1 | grep -E "(ecommerce|error|Error)" | head -20</automated>
  </verify>
  <done>
- Layout metadata title contains "B2B Product Sourcing" and description mentions Puno Filter and Smart Sync
- Inter font variable is loaded and applied to body className
- globals.css imported so BYU color tokens work in the ecommerce route group
- Build passes for this file
  </done>
</task>

<task type="auto">
  <name>Task 2: Build full ecommerce landing page (SUB-03)</name>
  <files>src/app/(ecommerce)/ecommerce/page.tsx</files>
  <action>
Replace the entire contents of `src/app/(ecommerce)/ecommerce/page.tsx`. No "use client" directive — pure Server Component. No Framer Motion. No new npm packages. Read UI-SPEC.md before writing (it is in the context section above).

Top-level imports:
```typescript
import { Globe, Factory, Handshake, Ruler, Package, Building2, ArrowRight } from "lucide-react";
import { siteConfig } from "@/data/site-config";
```

Implement all 8 sections. Detailed spec for each section:

**SECTION 1 — Header / Nav**
Outer: `<header className="sticky top-0 z-50 bg-white border-b border-slate-200">`
Inner: `<div className="max-w-6xl mx-auto px-6 h-16 flex items-center justify-between">`
Left: logo text `<span className="text-base font-semibold text-byu-navy">Philip Sun — Global Trading</span>`
Right: anchor to `mailto:${siteConfig.email}` with classes `bg-byu-navy text-white px-5 py-2 rounded-lg text-sm font-medium hover:bg-byu-navy/90 transition-colors focus-visible:ring-2 focus-visible:ring-byu-blue focus-visible:outline-none`
Button text: "Contact to Inquire"

**SECTION 2 — Hero**
Outer: `<section className="py-24 md:py-32 bg-white">`
Inner: `<div className="max-w-6xl mx-auto px-6">`
Eyebrow p: `className="text-sm font-mono text-byu-blue uppercase tracking-widest mb-6"` — text "B2B · Global Wholesale"
H1: `className="font-bold text-byu-navy mb-6 leading-tight"` with inline style `fontSize: "clamp(2rem, 5vw, 3.5rem)"` and `fontFamily: "var(--font-playfair, serif)"` — text "Global-Grade Products. Direct from Manufacturer." (break between lines with br tag)
Sub p: `className="text-xl text-slate-600 mb-10 max-w-2xl"` — "We source and distribute commercial water filtration and smart home systems to buyers, specifiers, and distributors worldwide. No retail. Direct deals only."
CTA wrapper div: `className="flex flex-wrap gap-4"`
Primary CTA: anchor to `mailto:${siteConfig.email}` — "Contact to Inquire" — `bg-byu-navy text-white px-8 py-4 rounded-lg text-base font-medium hover:bg-byu-navy/90 transition-colors focus-visible:ring-2 focus-visible:ring-byu-blue focus-visible:outline-none`
Secondary CTA: anchor href="#products" — "View Products" with ArrowRight icon — `text-byu-blue text-base font-medium underline-offset-2 hover:underline flex items-center gap-1 py-4`

**SECTION 3 — Trust Bar**
Outer: `<section className="py-10 border-y border-slate-100 bg-slate-50">`
Inner: `<div className="max-w-6xl mx-auto px-6 grid grid-cols-1 md:grid-cols-3 gap-8">`
Three items, each: `<div className="flex flex-col items-start gap-2">` with icon (w-6 h-6 text-byu-blue aria-hidden="true"), bold p for heading (font-semibold text-byu-navy), small p for sub (text-sm text-slate-500):
  Item 1: Globe icon / "Global Distribution" / "Shipping to buyers in North America, Europe, and Asia-Pacific"
  Item 2: Factory icon / "Direct Sourcing" / "Chinese manufacturers with verified quality standards"
  Item 3: Handshake icon / "Deal-Based Pricing" / "All pricing negotiated directly — no hidden markups"

**SECTION 4 — Products**
Outer: `<section id="products" className="py-20 bg-white">`
Inner: `<div className="max-w-6xl mx-auto px-6">`
H2: `className="text-3xl font-bold text-byu-navy mb-3"` — "Our Product Lines"
Sub p: `className="text-lg text-slate-600 mb-12"` — "Two complementary businesses serving B2B buyers in filtration and smart home markets."
Grid: `<div className="grid grid-cols-1 md:grid-cols-2 gap-6">`

Puno Filter card structure:
```
<div className="rounded-xl bg-white shadow-sm border border-slate-200 motion-safe:hover:shadow-md motion-safe:hover:scale-[1.01] transition-all duration-150 flex flex-col min-h-[420px]">
  <div className="h-1.5 bg-byu-navy rounded-t-xl" />
  <div className="p-8 flex flex-col flex-1">
    <h3 className="text-2xl font-bold text-byu-navy mb-1">Puno Filter</h3>
    <p className="text-base font-medium text-slate-500 mb-4">Commercial Water Filtration Systems</p>
    <p className="text-base text-slate-700 leading-relaxed flex-1">
      Industrial and commercial-grade water filtration systems sourced from verified Chinese
      manufacturers. We supply large-scale filtration solutions to buyers across North America,
      Europe, and Asia-Pacific — distributed globally on a B2B wholesale basis.
    </p>
    <div className="flex flex-wrap gap-2 mt-6 mb-6">
      {["Commercial Grade", "B2B Wholesale", "Global Distribution"].map((tag) => (
        <span key={tag} className="inline-flex items-center px-3 py-1 rounded-full text-xs font-medium bg-byu-sky text-byu-navy">{tag}</span>
      ))}
    </div>
    <a href={`mailto:${siteConfig.email}?subject=Puno Filter Inquiry`}
       className="text-byu-blue font-medium hover:underline inline-flex items-center gap-1 focus-visible:ring-2 focus-visible:ring-byu-blue focus-visible:outline-none rounded">
      Inquire about Puno Filter <ArrowRight className="w-4 h-4" aria-hidden="true" />
    </a>
  </div>
</div>
```

Smart Sync card: same structure, accent bar uses `bg-byu-blue` instead of `bg-byu-navy`.
- H3: "Smart Sync"
- Sub: "Smart Home Systems & Automation"
- Body: "Smart home technology — cameras, automation controllers, and home integration systems — sourced from Chinese manufacturers and positioned for the professional specification market. Ideal for interior designers, architects, and specifiers equipping residential and commercial projects."
- Tags: "Smart Home" / "Designer Spec" / "Professional Grade"
- CTA href: `mailto:${siteConfig.email}?subject=Smart Sync Inquiry` — text "Inquire about Smart Sync"

**SECTION 5 — Who We Serve**
Outer: `<section className="py-20 bg-slate-50">`
Inner: `<div className="max-w-6xl mx-auto px-6">`
H2: "Who We Work With" — `className="text-3xl font-bold text-byu-navy mb-3"`
Grid: `<div className="grid grid-cols-1 sm:grid-cols-3 gap-4 mt-10">`
Three cards, each `<div className="p-6 rounded-xl border border-slate-200 bg-white">`:
  Card 1: Ruler icon (w-10 h-10 text-byu-blue mb-4 aria-hidden) / H3 "Architects & Designers" / p "Specifying smart home systems for residential and commercial projects. Get product specs and pricing direct."
  Card 2: Package icon / H3 "Distributors & Wholesalers" / p "Looking to carry filtration or smart home products in your market. Let's discuss volume terms."
  Card 3: Building2 icon / H3 "Commercial Buyers" / p "Procurement for large-scale facilities and commercial properties. Custom arrangement available."
Card icon classes: `w-10 h-10 text-byu-blue mb-4`
H3 classes: `text-lg font-semibold text-byu-navy`
p classes: `text-sm text-slate-600 leading-relaxed mt-2`

**SECTION 6 — How It Works**
Outer: `<section className="py-20 bg-white">`
Inner: `<div className="max-w-6xl mx-auto px-6">`
H2: "Simple Process. Direct Deals." — `className="text-3xl font-bold text-byu-navy mb-3"`
Steps grid: `<div className="grid grid-cols-1 md:grid-cols-3 gap-8 mt-10">`
Three steps, each `<div className="flex flex-col gap-3">`:
  Step 01: span "01" (text-5xl font-bold text-byu-sky) / H3 "Send an inquiry" (text-xl font-semibold text-byu-navy) / p "Email Philip directly with your product interest and quantity requirements." (text-sm text-slate-600)
  Step 02: "02" / "Discuss terms" / "Pricing, MOQ, shipping, and timeline are negotiated directly — no middlemen."
  Step 03: "03" / "Ship globally" / "Products ship from manufacturer to your destination. All logistics coordinated."

**SECTION 7 — Contact CTA**
Outer: `<section className="py-20 bg-byu-navy">`
Inner: `<div className="max-w-6xl mx-auto px-6 text-center">`
H2: "Ready to source at scale?" — `className="text-4xl font-bold text-white"`
Sub p: "Get in touch and Philip will respond within 24 hours to discuss your requirements." — `className="text-lg text-byu-sky mt-4 max-w-xl mx-auto"`
CTA anchor to `mailto:${siteConfig.email}` — "Contact Philip" with ArrowRight — `className="inline-block mt-8 bg-white text-byu-navy px-8 py-4 rounded-lg font-semibold hover:bg-byu-sky transition-colors focus-visible:ring-2 focus-visible:ring-white focus-visible:outline-none"`

**SECTION 8 — Footer**
`<footer className="py-8 border-t border-slate-200 bg-white">`
`<p className="text-sm text-slate-500 text-center">Philip Sun · Global Trading &nbsp;|&nbsp; © {new Date().getFullYear()} &nbsp;|&nbsp; ecommerce.philipsun.com</p>`

**Accessibility requirements** (per UI-SPEC §6):
- All Lucide icons: aria-hidden="true" (all are paired with visible text labels)
- All anchor CTAs: descriptive text (no "click here")
- Focus rings: focus-visible:ring-2 focus-visible:ring-byu-blue focus-visible:outline-none on all interactive elements
- Heading hierarchy: H1 (hero) then H2 (section headers) then H3 (card titles within sections)
- Use motion-safe: variant on card hover scale/shadow (already shown in Puno Filter card above)

The page must have no "use client" directive. All data is static — no server fetching needed.
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -20</automated>
  </verify>
  <done>
- npm run build exits 0 — no TypeScript or JSX errors
- Page component is a default export RSC (no "use client")
- All 8 sections present: Header, Hero, Trust Bar, Products, Who We Serve, How It Works, Contact CTA, Footer
- Both product cards present: Puno Filter (navy accent bar) and Smart Sync (blue accent bar)
- All CTA anchors use mailto siteConfig.email — no hardcoded email address in page.tsx
- No pricing shown anywhere on the page
- All Lucide icons have aria-hidden="true" and are paired with visible text
- Page uses BYU design tokens (byu-navy, byu-blue, byu-sky) — no hardcoded hex colors except bg-[#F8FAFC] for page background
  </done>
</task>

</tasks>

<verification>
Run `npm run build`. If build succeeds, the page is valid RSC with no TypeScript errors.

Start dev server (`npm run dev`) and navigate to `http://localhost:3000/ecommerce`. Confirm:
1. All 8 sections render in order
2. Both product cards show with correct accent bar colors (navy for Puno, blue for Smart Sync)
3. "Contact to Inquire" header CTA and hero CTA are mailto links to siteConfig.email
4. No pricing is shown anywhere on the page
5. Page is readable on mobile viewport (open DevTools, set to iPhone SE 375px width)
6. Heading hierarchy is correct: one H1, multiple H2s, H3s inside cards
</verification>

<success_criteria>
1. `npm run build` exits 0
2. Page implements all 8 sections from the UI-SPEC
3. Both product lines (Puno Filter, Smart Sync) are displayed with accurate copy per UI-SPEC §4.4
4. Every CTA links to `mailto:${siteConfig.email}` — siteConfig is imported, no hardcoded email
5. No pricing, shopping cart, or retail language present anywhere on the page
6. layout.tsx metadata updated: title includes "B2B Product Sourcing", description mentions Puno Filter and Smart Sync
7. Responsive: single-column on mobile, 2-col products grid on md+, 3-col audience cards on sm+
</success_criteria>

<output>
After completion, create `.planning/phases/04-geo-and-ecommerce-subdomain/04-03-SUMMARY.md` following the summary template at `@$HOME/.claude/get-shit-done/templates/summary.md`.
</output>
