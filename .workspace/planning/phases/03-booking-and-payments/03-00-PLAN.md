---
phase: 03-booking-and-payments
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - src/app/__tests__/booking-form.test.tsx
  - src/app/__tests__/availability.test.ts
  - src/app/__tests__/booking.test.tsx
  - src/app/__tests__/webhook.test.ts
autonomous: true
requirements: [BUG-03, BUG-01, PHOTO-03, PHOTO-04]

must_haves:
  truths:
    - "All four test files exist and contain failing test stubs (RED phase)"
    - "Each test file imports the module it will test (even if that module does not exist yet)"
    - "npx vitest run src/app/__tests__/ completes without crashing (tests may fail, but must not throw unrecoverable errors)"
  artifacts:
    - path: "src/app/__tests__/booking-form.test.tsx"
      provides: "BUG-03 test stubs — Set immutability in BookingForm month navigation"
      contains: "setLoadedMonths"
    - path: "src/app/__tests__/availability.test.ts"
      provides: "BUG-01 test stubs — all-day event slot blocking + Mountain Time day bucketing"
      contains: "getAvailableSlots"
    - path: "src/app/__tests__/booking.test.tsx"
      provides: "PHOTO-03 test stubs — PhotographyBookingForm step render + navigation + ?pkg= pre-selection"
      contains: "PhotographyBookingForm"
    - path: "src/app/__tests__/webhook.test.ts"
      provides: "PHOTO-04 test stubs — webhook handler idempotency + email trigger"
      contains: "checkout.session.completed"
  key_links: []
---

<objective>
Create Wave 0 test stubs (TDD RED phase) for all Phase 3 behavioral requirements.

Purpose: Establish failing tests BEFORE implementation so executors can run RED-GREEN cycles. Every requirement in Phase 3 (BUG-03, BUG-01, PHOTO-03, PHOTO-04) gets at least one test stub.

Output: Four test files with `it.todo()` or `it.skip()` stubs that describe expected behavior. Tests reference the modules they will test (via import or mock) but are expected to fail or be skipped at this stage.
</objective>

<execution_context>
@/Users/philipsun/.claude/get-shit-done/workflows/execute-plan.md
@/Users/philipsun/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-booking-and-payments/03-VALIDATION.md
@.planning/phases/03-booking-and-payments/03-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create BUG-03 and BUG-01 test stubs</name>
  <files>src/app/__tests__/booking-form.test.tsx, src/app/__tests__/availability.test.ts</files>
  <read_first>
    - src/components/booking/BookingForm.tsx
    - src/lib/availabilityService.ts
    - src/lib/serverCalendar.ts
    - src/test/setup.ts
    - src/app/__tests__/home.test.tsx
  </read_first>
  <action>
**File 1: `src/app/__tests__/booking-form.test.tsx`** (BUG-03)

Create a test file for the `BookingForm` component's `loadedMonths` Set immutability fix. The test must:

1. Import `BookingForm` from `@/components/booking/BookingForm`
2. Mock `@/lib/icalendarService` (export `fetchICloudEvents` returning empty array)
3. Mock `@/lib/availabilityService` (export `getAvailableSlots` returning mock slots)
4. Mock `date-fns-tz` `format` to return a stable string
5. Mock `framer-motion` with `{ motion: { div: 'div', span: 'span' }, useReducedMotion: () => false }`

Write these test stubs (use `it.todo` or `it` with a comment `// Wave 1 implementation`):

```
describe('BookingForm — BUG-03: loadedMonths immutability', () => {
  it.todo('does not call loadedMonths.add() directly — uses setLoadedMonths functional update')
  it.todo('navigating to a new month and back does not re-fetch already-loaded months')
})
```

**File 2: `src/app/__tests__/availability.test.ts`** (BUG-01)

Create a test file for `getAvailableSlots` from `@/lib/availabilityService`. No mocks needed — this is a pure function. Write these test stubs:

```
describe('getAvailableSlots — BUG-01: all-day event blocking', () => {
  it.todo('returns no slots when an all-day blocking event covers the entire day')
  it.todo('correctly blocks slots when event times are in UTC but day is evaluated in Mountain Time')
  it.todo('returns slots for a day with no events')
  it.todo('returns no slots on weekends regardless of events')
})
```

Note for BUG-01 tests: The fix will be in `serverCalendar.ts` (day bucketing) which feeds events to `getAvailableSlots`. The unit test at this level validates that `getAvailableSlots` correctly blocks slots when given properly-bucketed events. The `serverCalendar.ts` timezone bucketing fix is tested implicitly via integration.
  </action>
  <verify>
    <automated>cd "/Users/philipsun/Documents/personal websit" && npx vitest run src/app/__tests__/booking-form.test.tsx src/app/__tests__/availability.test.ts --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - File `src/app/__tests__/booking-form.test.tsx` exists and contains `describe('BookingForm`
    - File `src/app/__tests__/booking-form.test.tsx` contains `loadedMonths` or `setLoadedMonths`
    - File `src/app/__tests__/availability.test.ts` exists and contains `describe('getAvailableSlots`
    - File `src/app/__tests__/availability.test.ts` contains `all-day` in a test description
    - Both files import from their respective source modules
    - `npx vitest run src/app/__tests__/booking-form.test.tsx src/app/__tests__/availability.test.ts` exits without crashing (tests may be skipped/todo)
  </acceptance_criteria>
  <done>Two test stub files exist. BUG-03 stubs test Set immutability. BUG-01 stubs test all-day event slot blocking. Both files run without crashing.</done>
</task>

<task type="auto">
  <name>Task 2: Create PHOTO-03 and PHOTO-04 test stubs</name>
  <files>src/app/__tests__/booking.test.tsx, src/app/__tests__/webhook.test.ts</files>
  <read_first>
    - src/data/photography.ts
    - src/db/schema.ts
    - src/lib/icsService.ts
    - src/test/setup.ts
    - .planning/phases/03-booking-and-payments/03-VALIDATION.md
  </read_first>
  <action>
**File 1: `src/app/__tests__/booking.test.tsx`** (PHOTO-03)

Create a test file for the `PhotographyBookingForm` component (which does not exist yet — will be built in Plan 03-02). The test must:

1. Import will fail until component is built — use `vi.mock` or a try/catch pattern. Alternatively, create a minimal stub file `src/components/booking/PhotographyBookingForm.tsx` that exports `function PhotographyBookingForm() { return null; }` as a placeholder.
2. Mock `next/navigation` with `useSearchParams` returning a URLSearchParams mock
3. Mock `@/lib/availabilityService` (getAvailableSlots returning mock slots)
4. Mock `@/data/photography` with the real `photographyPackages` array (import directly — it's static data)

Write these test stubs:

```
describe('PhotographyBookingForm — PHOTO-03: multi-step booking flow', () => {
  it.todo('renders step 1 with package selection when no ?pkg= param')
  it.todo('pre-selects package when ?pkg=portrait-session is in URL')
  it.todo('advances from step 1 to step 2 (date) when Continue is clicked with a package selected')
  it.todo('advances from step 2 to step 3 (time) when a date is selected')
  it.todo('advances from step 3 to step 4 (details) when a time slot is selected')
  it.todo('shows "Proceed to Payment" button on step 4 instead of "Continue"')
  it.todo('disables Continue button when no package is selected on step 1')
})
```

**File 2: `src/app/__tests__/webhook.test.ts`** (PHOTO-04)

Create a test file for the Stripe webhook handler at `src/app/api/webhooks/stripe/route.ts` (does not exist yet). Test the POST handler logic:

1. Mock `stripe` module — `stripe.webhooks.constructEvent` returns a mock event
2. Mock `@/db` — `db.select`, `db.insert`, `db.delete` returning mock results
3. Mock `resend` — `resend.emails.send` returning `{ id: 'test' }`
4. Mock `@/lib/icsService` — `generateICSContent` returning a string

Write these test stubs:

```
describe('Stripe webhook handler — PHOTO-04: booking confirmation', () => {
  it.todo('returns 400 when stripe-signature header is missing')
  it.todo('returns 400 when signature verification fails')
  it.todo('creates booking row on checkout.session.completed event')
  it.todo('creates payment row alongside booking')
  it.todo('sends confirmation email with ICS attachment after successful booking')
  it.todo('does NOT create duplicate booking when webhook is retried (idempotency)')
  it.todo('deletes pending reservation after booking is confirmed')
  it.todo('sends notification email to siteConfig.email')
})
```
  </action>
  <verify>
    <automated>cd "/Users/philipsun/Documents/personal websit" && npx vitest run src/app/__tests__/booking.test.tsx src/app/__tests__/webhook.test.ts --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - File `src/app/__tests__/booking.test.tsx` exists and contains `describe('PhotographyBookingForm`
    - File `src/app/__tests__/booking.test.tsx` contains `pkg=portrait-session` or `?pkg=` in a test description
    - File `src/app/__tests__/booking.test.tsx` contains `Proceed to Payment` in a test description
    - File `src/app/__tests__/webhook.test.ts` exists and contains `describe('Stripe webhook`
    - File `src/app/__tests__/webhook.test.ts` contains `idempotency` in a test description
    - File `src/app/__tests__/webhook.test.ts` contains `ICS attachment` in a test description
    - Both files run without crashing (tests may be skipped/todo)
  </acceptance_criteria>
  <done>Two test stub files exist. PHOTO-03 stubs cover multi-step form rendering and navigation. PHOTO-04 stubs cover webhook idempotency, email trigger, and ICS attachment. Both run without crashing.</done>
</task>

</tasks>

<verification>
All four test files exist in `src/app/__tests__/`:
- `booking-form.test.tsx` (BUG-03)
- `availability.test.ts` (BUG-01)
- `booking.test.tsx` (PHOTO-03)
- `webhook.test.ts` (PHOTO-04)

Run: `npx vitest run src/app/__tests__/ --reporter=verbose` — all tests are either `.todo` or skipped. No crashes.
</verification>

<success_criteria>
- Four test files created, one per requirement
- Each file has meaningful test descriptions matching VALIDATION.md task map
- `npx vitest run src/app/__tests__/` completes without unrecoverable errors
- Test stubs are RED (failing or todo) — none pass yet
</success_criteria>

<output>
After completion, create `.planning/phases/03-booking-and-payments/03-00-SUMMARY.md`
</output>
